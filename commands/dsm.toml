description = "Alias for /ds-mode. Activate DS Mode and pick a mode (lite | full | visual | off)."
prompt = """
Activate DS Mode at level "{{args}}" (default `full` when args are empty). Same behavior as `/ds-mode`. See `commands/ds-mode.toml` for the full description.

Confirm in one short line and proceed.
"""
